epochs: 50
batch_size: 8
learning_rate: 1.0e-4
weight_decay: 1.0e-4
lr_scheduler: cosine
clip_grad_norm: 1.0
num_workers: 4
pin_memory: true

# Warm-up: backbone frozen for first N epochs, then fully unfrozen
backbone_warmup_epochs: 5
